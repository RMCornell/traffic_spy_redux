# specify which database and adapter to use for which environment

development:
  adapter: postgresql
  database: a-traffic-spy-development

test:
  adapter: postgresql
  database: a-traffic-spy-test

production:
  adapter: postgresql
  database: a-traffic-spy-production
